# go-graphql-client-todos

A POC GraphQL client for subscriptions.

## How To

Run this command in one or more terminals.  The todos will appear in the logs as
they are created by other clients.

```bash
make run
```
